machine:
  node:
    version: 5.6.0

dependencies:
  override:
    - echo "//registry.npmjs.org/:_authToken=\${NPM_AUTH_TOKEN}" > ~/.npmrc
    - npm install

deployment:
  production:
    branch: master
    heroku:
      appname: lg-product-metrics
